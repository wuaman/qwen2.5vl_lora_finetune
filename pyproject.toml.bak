[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my_project"
version = "0.1.0"
description = "A modern Python project"
dependencies = []
requires-python = ">=3.10"

# ==============================
# 🛠️ Ruff 全局配置
# ==============================
[tool.ruff]
# 启用预览功能
preview = true

# 目标 Python 版本
target-version = "py310"

# 自动修复
fix = true
unsafe-fixes = false

# --- 更新：行长修改为 120 ---
line-length = 120

# 缩进宽度（视觉宽度为4，具体用tab还是空格由 format 决定）
indent-width = 4

# 忽略的路径
extend-exclude = [
    ".git",
    "__pycache__",
    "venv",
    ".venv",
    "build",
    "dist",
    "notebooks",
]

# ==============================
# 🔍 Lint 规则配置
# ==============================
[tool.ruff.lint]
# 启用的规则集
# 我保留了之前较全的列表，因为它已经包含了你要求的 F, E, W, UP
select = [
    "E", "W", "F",
    "I",
    "UP",
    "C4",
    "TID",
    "ARG",
    "PTH",
]

# --- 更新：添加 F401 (忽略未使用的导入) ---
# E501: 行长交给 formatter
# D: 忽略文档缺失
# F401: 忽略未使用的 import
ignore = [
    "E501", 
    "D", 
    "F401" ,
    "W191",
    "W293",
]

[tool.ruff.lint.per-file-ignores]
"tests/**.py" = ["S101", "ARG", "PLR0913"]
"scripts/*.py" = ["T20"]

# ==============================
# 🧹 isort 配置
# ==============================
[tool.ruff.lint.isort]
known-first-party = ["my_project"]
combine-as-imports = true
force-sort-within-sections = true

# ==============================
# ✨ Formatter 配置
# ==============================
[tool.ruff.format]
preview = true

# 使用单引号
quote-style = "single"

# --- 更新：缩进使用 Tab ---
indent-style = "tab"

# --- 更新：启用 docstring 代码块格式化 ---
docstring-code-format = true

# 保持默认（兼容 Black 的逗号行为）
skip-magic-trailing-comma = false

# ==============================
# 🔤 Pyright 配置
# ==============================
[tool.pyright]
typeCheckingMode = "basic"
pythonVersion = "3.10"
include = ["src", "tests"]
exclude = [
    "**/node_modules",
    "**/__pycache__",
    ".venv",
    "venv",
    "build",
    "dist",
    "notebooks",
]
reportMissingTypeStubs = "none"
reportUnusedImport = "warning"
reportUnusedVariable = "warning"